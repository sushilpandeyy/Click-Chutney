# ==============================================
# 🍛 ClickChutney Analytics - Environment Variables
# ==============================================

# Authentication Secrets (Generate with: openssl rand -base64 32)
# Keep these secret and never commit them to version control
AUTH_SECRET=your-32-character-secret-key
BETTER_AUTH_SECRET=your-32-character-secret-key
NEXTAUTH_URL=http://localhost:3000

# GitHub OAuth (Create at: https://github.com/settings/developers)
# Create a new OAuth App with:
# - Application name: ClickChutney Analytics
# - Homepage URL: http://localhost:3000 (or your production URL)  
# - Authorization callback URL: http://localhost:3000/api/auth/callback/github
# - Enable "user:email" scope for email access
GITHUB_CLIENT_ID=your-github-client-id
GITHUB_CLIENT_SECRET=your-github-client-secret

# MongoDB Database (Get from MongoDB Atlas)
# Format: mongodb+srv://username:password@cluster.mongodb.net/database_name?retryWrites=true&w=majority
DATABASE_URL="mongodb+srv://username:password@cluster.mongodb.net/clickchutney?retryWrites=true&w=majority"

# Optional: For production deployment
NEXT_PUBLIC_BASE_URL=http://localhost:3000

# Optional: Custom database name (defaults based on NODE_ENV)
# DATABASE_NAME=clickchutney_dev

# ==============================================
# Development vs Production
# ==============================================
# Development: Uses clickchutney_dev database
# Production: Uses clickchutney_prod database
# 
# For local development:
# 1. Copy this file to .env
# 2. Fill in your actual values
# 3. Never commit .env to version control
#
# For production deployment (Vercel):
# 1. Set these in Project Settings → Environment Variables
# 2. Update NEXTAUTH_URL to your production domain
# 3. Update GitHub OAuth callback URL to production domain